# Echoserver - Configuration Reference
# The echoserver uses raw manifests (manifests/echoserver/) instead of Helm.
# Modify the deployment, service, and ingress directly.
#
# Image: ealen/echo-server:0.9.2
# Ingress host: echoserver.localhost
# Replicas: 1
